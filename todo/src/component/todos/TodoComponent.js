import LogoutButton from "../../button/LogoutButton";

function TodoComponent({id}){
    console.log(id)
    return(
        <div>
             main
            <LogoutButton/>
        </div>
    )
}

export default TodoComponent;